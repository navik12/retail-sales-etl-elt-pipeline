# Retail Sales ETL Pipeline — dependencies

# --- Core ETL (Levels 1-3) ---
pandas>=2.0
requests>=2.31
SQLAlchemy>=2.0
psycopg2-binary>=2.9

# --- Testing / CI (Level 5) ---
pytest>=8.0

# --- Orchestration (Level 4) ---
# Airflow is heavy and best installed with its official constraints file,
# so we install it when we reach Level 4 rather than here. Kept listed for
# documentation. To install later:
#   pip install "apache-airflow==2.10.4" \
#     --constraint "https://raw.githubusercontent.com/apache/airflow/constraints-2.10.4/constraints-3.12.txt"
# apache-airflow>=2.10
